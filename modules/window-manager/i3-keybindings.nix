# i3 keybindings configuration
# Add custom keybindings here. Options and base config are in i3-config.nix.
#
# Example keybinding additions:
#   xsession.windowManager.i3.config.keybindings = lib.mkOptionDefault {
#     "${modifier}+p" = "exec ${config.gui.i3.commands.powerProfile}";
#     "${modifier}+b" = "exec ${config.gui.i3.commands.emoji}";
#   };
_: {
  # Currently using Home Manager's default i3 keybindings.
  # Custom keybindings can be added here when needed.
}
