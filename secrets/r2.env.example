# Template for Cloudflare R2 per-user env (sops-managed, dotenv format)
#
# Usage:
#   1. Copy to secrets/r2.env
#   2. Fill in your credentials/account info
#   3. Encrypt in-place with sops per docs/sops/README.md:
#        sops -e -i secrets/r2.env
#
# The Home-Manager module places the decrypted file at:
#   ~/.config/cloudflare/r2/env (0400)
# Used by wrappers: r2 (rclone) and r2s5 (s5cmd)

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Preferred: set account and let tools derive endpoint automatically
R2_ACCOUNT_ID=

# Or set an explicit endpoint instead (do not set both)
# AWS_ENDPOINT_URL=https://<ACCOUNT_ID>.r2.cloudflarestorage.com

