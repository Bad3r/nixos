---
# Template for Cloudflare ACME DNS token (sops-managed)
#
# Usage:
#   1. Copy to secrets/cf-api-token.yaml
#   2. Replace the placeholder value below
#   3. Encrypt in-place with sops per docs/sops-nixos.md:
#        sops -e -i secrets/cf-api-token.yaml
#
# Notes:
# - The value should be the raw token string (no key=value prefix)
# - The system module will render it at /run/secrets/cf-api-token

cf_api_token: "<REPLACE_WITH_CLOUDFLARE_DNS_API_TOKEN>"

