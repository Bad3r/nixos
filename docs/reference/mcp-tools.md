# MCP Tools

Generated from `flake.lib.agents.mcp.servers`. `Default Clients` shows which Nix-managed clients enable each server out of the box. Use `/mcp` to inspect current runtime availability.

| Tool                  | Default Clients    | Primary Use                                                 | Access Notes                                                                                                            | Example                                                                                                                    |
| --------------------- | ------------------ | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `cfbindings`          | None               | Inspect Cloudflare bindings for Workers and Pages projects. | Available in the catalog but not enabled by default for Codex or Claude Code.                                           | Use `/mcp` to inspect the available `cfbindings` tools.                                                                    |
| `cfbrowser`           | Codex, Claude Code | Render and capture live webpages.                           | Useful for verifying UI changes against deployed sites.                                                                 | `cfbrowser get-url-html --url <page>`                                                                                      |
| `cfbuilds`            | None               | Inspect Cloudflare builds and deployment activity.          | Available in the catalog but not enabled by default for Codex or Claude Code.                                           | Use `/mcp` to inspect the available `cfbuilds` tools.                                                                      |
| `cfcontainers`        | None               | Inspect Cloudflare Containers projects and instances.       | Available in the catalog but not enabled by default for Codex or Claude Code.                                           | Use `/mcp` to inspect the available `cfcontainers` tools.                                                                  |
| `cfdocs`              | Codex, Claude Code | Search Cloudflare documentation.                            | Use for Workers, R2, Zero Trust, and other Cloudflare services.                                                         | `cfdocs search --query "Workers KV"`                                                                                       |
| `cfgraphql`           | None               | Run GraphQL queries against Cloudflare analytics endpoints. | Available in the catalog but not enabled by default for Codex or Claude Code.                                           | Use `/mcp` to inspect the available `cfgraphql` tools.                                                                     |
| `cfobservability`     | None               | Query Cloudflare observability and logging data.            | Available in the catalog but not enabled by default for Codex or Claude Code.                                           | Use `/mcp` to inspect the available `cfobservability` tools.                                                               |
| `cfradar`             | None               | Query Cloudflare Radar internet telemetry and trends.       | Available in the catalog but not enabled by default for Codex or Claude Code.                                           | Use `/mcp` to inspect the available `cfradar` tools.                                                                       |
| `chrome-devtools`     | Codex, Claude Code | Control and inspect a live Chrome browser session.          | Local wrapper auto-detects Chrome or Chromium, passes `--executablePath`, and keeps `--isolated --no-usage-statistics`. | `npx -y chrome-devtools-mcp@latest --executablePath="$(command -v google-chrome-stable)" --isolated --no-usage-statistics` |
| `context7`            | Codex, Claude Code | Look up library IDs and documentation for coding tasks.     | Requires the Context7 API key provisioned at the standard local secret path.                                            | `context7 resolve-library-id --name <library>`                                                                             |
| `deepwiki`            | Codex, Claude Code | Browse repository knowledge bases.                          | Pass `owner/repo` to fetch docs.                                                                                        | `deepwiki read_wiki_structure --repo owner/repo`                                                                           |
| `memory`              | Codex              | Persist lightweight graph memory across runs.               | Enabled by default only for Codex.                                                                                      | `memory search_nodes --query "token endpoint"`                                                                             |
| `openaiDeveloperDocs` | Codex              | Search OpenAI developer docs and API references.            | Enabled by default only for Codex.                                                                                      | `codex mcp add openaiDeveloperDocs --url https://developers.openai.com/mcp`                                                |
| `playwright`          | Codex, Claude Code | Automate browser interactions for UI and E2E checks.        | Local wrapper auto-detects Chrome or Chromium, passes `--executable-path`, and keeps an `--isolated` profile.           | `npx -y @playwright/mcp@latest --executable-path="$(command -v google-chrome-stable)" --browser=chrome --isolated`         |
| `sequential-thinking` | Codex, Claude Code | Record structured reasoning steps for complex tasks.        | Useful for debugging, planning, and other nontrivial workflows.                                                         | `sequentialthinking start`                                                                                                 |
