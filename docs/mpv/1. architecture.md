# Architecture

mpv is composed in two cooperating layers: a NixOS app module that manages
system-wide install and a Home Manager app module that writes the user-level
configuration. A third orthogonal layer, the `media-toolchain` bundle, can
install mpv even when the dedicated app module is disabled. This page explains
the layering, the namespaces involved, and the gating contract that connects
them.

## Layered Composition

| Layer            | Module location                    | Aggregator namespace                      | What it does                                                                                                           |
| ---------------- | ---------------------------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| NixOS app module | `modules/apps/mpv.nix`             | `flake.nixosModules.apps.mpv`             | Adds `pkgs.mpv` and a small set of extras to `environment.systemPackages` when `programs.mpv.extended.enable` is true. |
| HM app module    | `modules/hm-apps/mpv.nix`          | `flake.homeManagerModules.apps.mpv`       | Writes `mpv.conf`, `input.conf`, an `xdg.configFile` Lua hook, and a script list when the NixOS layer is enabled.      |
| Media bundle     | `modules/apps/media-toolchain.nix` | `flake.nixosModules.apps.media-toolchain` | Installs `pkgs.mpv` plus codecs and helpers. Independent of the dedicated mpv enable bit.                              |

## Aggregator Namespaces

There are two namespaces:

- `flake.nixosModules.apps.mpv`
- `flake.homeManagerModules.apps.mpv`

For background on how the two namespaces behave differently see
[../architecture/03-nixos-modules.md](../architecture/03-nixos-modules.md) and
[../architecture/04-home-manager.md](../architecture/04-home-manager.md).

## Option Namespace

The NixOS module declares `programs.mpv.extended` with the standard options
this repo uses for app modules:

| Option                                | Type             | Default                                              | Purpose                                                                                                                                   |
| ------------------------------------- | ---------------- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `programs.mpv.extended.enable`        | `bool`           | `false`                                              | Master switch. Controls system install and HM gating.                                                                                     |
| `programs.mpv.extended.package`       | package option   | `pkgs.mpv`                                           | The mpv package to install.                                                                                                               |
| `programs.mpv.extended.extraScripts`  | list of packages | `[ pkgs.mpvScripts.thumbfast ]`                      | Adds packages to `environment.systemPackages`. Note: see [scripts-and-bindings.md](<3. scripts-and-bindings.md>) for an important caveat. |
| `programs.mpv.extended.extraPackages` | list of packages | `[ pkgs.mpv-shim-default-shaders pkgs.open-in-mpv ]` | Additional mpv-related tools added to system packages.                                                                                    |

The HM layer does not introduce a parallel option namespace; it consumes the
NixOS `enable` flag through `osConfig` (see below) and otherwise drives the
upstream Home Manager `programs.mpv` module directly.

## Relation to the Media Bundle

`modules/apps/media-toolchain.nix` is an orthogonal app module that installs
`pkgs.mpv` along with `ffmpeg-full`, `imagemagick`, `ghostscript`, and the
GStreamer plugin sets. It is enabled on hosts that want a broad codec
toolchain. Because it adds `mpv` directly to `environment.systemPackages`, the
mpv binary is present even on hosts where `programs.mpv.extended.enable` is
`false`. In that scenario the user runs upstream-default mpv with no managed
configuration files. See [integrations.md](<4. integrations.md>) for the
implications.

TODO: Media bundle will be deprecated and replaced with independent modules

## Where to Look Next

- For the full set of values written into `mpv.conf`: [configuration.md](<2. configuration.md>).
- For how scripts are actually loaded (and the systemPackages-vs-HM-scripts
  distinction): [scripts-and-bindings.md](<3. scripts-and-bindings.md>).
- For MIME wiring, MPRIS, browser integration, and the playlist helper:
  [integrations.md](<4. integrations.md>).
